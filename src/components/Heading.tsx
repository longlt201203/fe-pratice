import styled from "styled-components";

const Title = styled.h1`
    font-size: 2rem;
    font-weight: bold;
`

const H1 = styled.h1`
    font-size: 1.6rem;
    font-weight: bold;
`

const Heading = {
    Title,
    H1
}

export default Heading;